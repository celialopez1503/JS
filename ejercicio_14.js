// ejercicio filtra el siguiente array de números creando uno con números para y otro con impares:

const lista = [251, 634, 89, 75, 1, 20, 1233, 65];

const controlpar = lista.filter( function (numerito){
    if (numerito%2 === 0){

    }return numerito  
    });

    console.log (numerito);
    

const controlImpar = lista.filter( function(number){
    if (number%2 !== 0){
        return number;
        
    }
});

console. log(number);




///REDUCE  volver a hacer

const palabras = ['ala', 'barco', 'cocina']

const longitud = palabras.reduce( function(acum, currentValue) 
if (acum.lenght > currentValue.lenght) {
    return (acum);   
};
else {
    (currentValue > acum)
    return (currentValue)
}
console.log(resultado)



